# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json
specVersion: "3.1"
metadata:
  name: wb.project1
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: destination_test
          url: http://tehsu161.serv.dev.dc.es.telefonica:443
          client: '500'
        app:
          name: ZFIORIRUEBA
          description: Caro prueba
          package: $TMP
          transport: ''
        exclude:
          - /test/
